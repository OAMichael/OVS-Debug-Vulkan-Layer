import sys
import os
import re
import enum

SCRIPT_PATH = os.path.abspath(__file__)
SCRIPT_DIR = os.path.dirname(SCRIPT_PATH)
ROOT_DIR = os.path.dirname(SCRIPT_DIR)
EXTERNAL_DIR = os.path.join(ROOT_DIR, 'External')
VK_HEADERS_DIR = os.path.join(EXTERNAL_DIR, 'Vulkan-Headers')
REGISTRY_DIR = os.path.join(VK_HEADERS_DIR, 'registry')
REGISTRY_PATH = os.path.join(REGISTRY_DIR, 'vk.xml')
VIDEO_REGISTRY_PATH = os.path.join(REGISTRY_DIR, 'video.xml')

sys.path.append(REGISTRY_DIR)

import reg
import xml.etree.ElementTree as etree
import vkconventions

from generator import OutputGenerator
from cgenerator import COutputGenerator, CGeneratorOptions


DEBUG_PARAM_TYPE_KIND = False

class ParamTypeKind(enum.IntEnum):
    PRIMITIVE                    = 0
    HANDLE                       = 1
    STRUCT                       = 2
    PRIMITIVE_PTR                = 3
    HANDLE_PTR                   = 4
    STRUCT_PTR                   = 5
    PRIMITIVE_STATIC_ARRAY_1D    = 6
    HANDLE_STATIC_ARRAY_1D       = 7
    STRUCT_STATIC_ARRAY_1D       = 8
    PRIMITIVE_DYNAMIC_ARRAY_1D   = 9
    HANDLE_DYNAMIC_ARRAY_1D      = 10
    STRUCT_DYNAMIC_ARRAY_1D      = 11
    PRIMITIVE_STATIC_ARRAY_2D    = 12
    HANDLE_STATIC_ARRAY_2D       = 13
    STRUCT_STATIC_ARRAY_2D       = 14
    PRIMITIVE_DYNAMIC_ARRAY_2D   = 15
    HANDLE_DYNAMIC_ARRAY_2D      = 16
    STRUCT_DYNAMIC_ARRAY_2D      = 17
    STRING                       = 18
    WSTRING                      = 19
    STRING_DYNAMIC_ARRAY_1D      = 20
    VOID_PTR                     = 21
    VOID_PTR_PTR                 = 22
    VOID_PTR_DYNAMIC_ARRAY_1D    = 23
    EXTERNAL_PTR                 = 24
    EXTERNAL_PTR_PTR             = 25
    FUNCTION_PTR                 = 26
    PNEXT                        = 27


BASE_TYPES = [
    'void',
    'char',
    'float',
    'double',
    'int8_t',
    'uint8_t',
    'int16_t',
    'uint16_t',
    'uint32_t',
    'uint64_t',
    'int32_t',
    'int64_t',
    'size_t',
    'int',
    'DWORD'
]

EXTERNAL_TYPES = [
    'ANativeWindow',
    'AHardwareBuffer',
    'OHNativeWindow',
    'OHBufferHandle',
    'OH_NativeBuffer',
    'CAMetalLayer',
    '_screen_context',
    '_screen_window',
    '_screen_buffer',
    'wl_display',
    'wl_surface',
    'SECURITY_ATTRIBUTES'
]

EXTERNAL_TYPES_PTR = [
    'MTLDevice_id',
    'MTLCommandQueue_id',
    'MTLBuffer_id',
    'MTLTexture_id',
    'IOSurfaceRef',
    'MTLSharedEvent_id',
    'VkRemoteAddressNV',
    'Display',
    'Window',
    'VisualID',
    'xcb_connection_t',
    'xcb_window_t',
    'xcb_visualid_t',
    'GgpStreamDescriptor',
    'GgpFrameToken',
    'zx_handle_t',
    'IDirectFBSurface',
    'IDirectFB',
    'RROutput',
    'HINSTANCE',
    'HWND',
    'HMONITOR',
    'HANDLE',
]


SPECIAL_INSTANCE_COMMANDS = [
    'vkCreateInstance',
    'vkGetInstanceProcAddr',
    'vkEnumerateInstanceExtensionProperties',
    'vkEnumerateInstanceLayerProperties',
    'vkEnumerateInstanceVersion'
]


args = ''.join(sys.argv)

autogen_message = f'''
/*
 * This file is autogenerated using command \'{args}\'
 *
 * Do not modify this file manually!
 */

'''


def getMacroForFilename(filename):
    basename = os.path.basename(filename)
    macro = re.sub(r'(?<!^)(?=[A-Z])', '_', basename).upper()
    return macro.replace('.', '_')

def getFirstParamType(cmdinfo):
    params = cmdinfo.elem.findall('param')
    if len(params) == 0:
        return None

    first_param = params[0]
    return first_param.find('type').text.strip()

def safeStr(s):
    if s is not None:
        return s
    return ''

def getNonAliasTypeElem(type, registry):
    typeElem = registry.lookupElementInfo(type, registry.typedict).elem
    typeAlias = registry.getAlias(typeElem, registry.typedict)
    while typeAlias:
        typeElem = registry.lookupElementInfo(typeAlias, registry.typedict).elem
        typeAlias = typeElem.get('alias')
    return typeElem

def patchLengthMember(length, prependStr):
    memberIdentifiers = re.findall(r'[a-zA-Z_]\w*', length)
    memberIdentifiers = set(memberIdentifiers)
    for id in memberIdentifiers:
        if id.isupper():
            continue
        length = length.replace(id, prependStr + id)
    return length

def patchDynamicArrayLengthMember(length, prependStr):
    # Freaking StdVideoH265HrdParameters::pSubLayerHrdParametersNal and StdVideoH265HrdParameters::pSubLayerHrdParametersVcl
    if length.startswith('*'):
        length = '1'

    ptr = False
    ptrCond = ''
    if '->' in length:
        arrowPos = length.find('->')
        finalMember = length[arrowPos + 2:]
        ptr = (finalMember[0] == 'p' and finalMember[1].isupper())
        length = prependStr + length
    else:
        ptr = (length[0] == 'p' and length[1].isupper())
        length = patchLengthMember(length, prependStr)

    if ptr:
        ptrCond += f' && {length}'
        length = '*' + length

    ptrCond += f' && {length} > 0'

    return (length, ptrCond)


def addVideoTree(registry, video_tree):
    video_reg = video_tree.getroot()

    for typeElem in video_reg.findall('types/type'):
        name = typeElem.get('name')
        if name is None:
            nameElem = typeElem.find('name')
            if nameElem is None or not nameElem.text:
                raise RuntimeError("Type without a name!")
            name = nameElem.text
            typeElem.set('name', name)

        typeInfo = reg.TypeInfo(typeElem)
        registry.typedict[name] = typeInfo

        alias = typeElem.get('alias')
        if alias:
            registry.aliasdict[name] = alias

    for group in video_reg.findall('enums'):
        name = group.get('name')
        groupInfo = reg.GroupInfo(group)
        registry.groupdict[name] = groupInfo

    for enums in video_reg.findall('enums'):
        required = (enums.get('type') is not None)
        type_name = enums.get('name')
        for enum in enums.findall('enum'):
            enumName = enum.get('name')
            enumInfo = reg.EnumInfo(enum)
            enumInfo.required = required
            registry.enumdict[enumName] = enumInfo
            registry.addEnumValue(enum, type_name)

    for feature in video_reg.findall('extensions/extension'):
        featureName = feature.get('name')
        featureInfo = reg.FeatureInfo(feature)
        registry.extdict[featureName] = featureInfo

        for elem in feature.findall('require'):
            for enum in elem.findall('enum'):
                groupName = enum.get('extends')
                if enum.get('value') or enum.get('bitpos') or enum.get('alias'):
                    enumName = enum.get('name')
                    enumInfo = reg.EnumInfo(enum)
                    registry.enumdict[enumName] = enumInfo
                    registry.addEnumValue(enum, groupName)


def getParamTypeKind(paramElem, registry):
    name = safeStr(paramElem.find('name').text).strip()
    type = safeStr(paramElem.find('type').text).strip()
    type_ptrs = safeStr(paramElem.find('type').tail).strip()
    type_brackets = safeStr(paramElem.find('name').tail).strip()
    for e in paramElem.findall('enum'):
        type_brackets += e.text + e.tail
    length = safeStr(paramElem.get('altlen')).strip()
    if length == '':
        length = safeStr(paramElem.get('len')).strip()

    if name == 'pNext':
        return ParamTypeKind.PNEXT
    if type == 'LPCWSTR':
        return ParamTypeKind.WSTRING

    typeElem = getNonAliasTypeElem(type, registry)
    category = safeStr(typeElem.get('category'))
    if category == 'funcpointer':
        return ParamTypeKind.FUNCTION_PTR

    stars = type_ptrs.count('*')
    brackets = type_brackets.count('[')

    if type in BASE_TYPES or type in EXTERNAL_TYPES or type in EXTERNAL_TYPES_PTR or category in ['basetype', 'enum', 'bitmask']:
        if stars == 0:
            if type in EXTERNAL_TYPES_PTR:
                return ParamTypeKind.EXTERNAL_PTR
            if brackets == 0:
                return ParamTypeKind.PRIMITIVE
            if brackets == 1:
                return ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D
            if brackets == 2:
                return ParamTypeKind.PRIMITIVE_STATIC_ARRAY_2D
        if stars == 1:
            if type == 'char':
                return ParamTypeKind.STRING
            if type == 'void' and length == '':
                return ParamTypeKind.VOID_PTR
            if type in EXTERNAL_TYPES:
                return ParamTypeKind.EXTERNAL_PTR
            if type in EXTERNAL_TYPES_PTR:
                return ParamTypeKind.EXTERNAL_PTR_PTR
            if length != '':
                return ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_1D
            return ParamTypeKind.PRIMITIVE_PTR
        if stars == 2:
            if type == 'char':
                return ParamTypeKind.STRING_DYNAMIC_ARRAY_1D
            if type == 'void':
                if length != '':
                    return ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D
                return ParamTypeKind.VOID_PTR_PTR
            if type in EXTERNAL_TYPES:
                return ParamTypeKind.EXTERNAL_PTR_PTR
            return ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_2D

    if category == 'handle':
        if stars == 0:
            if brackets == 0:
                return ParamTypeKind.HANDLE
            if brackets == 1:
                return ParamTypeKind.HANDLE_STATIC_ARRAY_1D
            if brackets == 2:
                return ParamTypeKind.HANDLE_STATIC_ARRAY_2D
        if stars == 1:
            if length != '':
                return ParamTypeKind.HANDLE_DYNAMIC_ARRAY_1D
            return ParamTypeKind.HANDLE_PTR
        if stars == 2:
            return ParamTypeKind.HANDLE_DYNAMIC_ARRAY_2D

    if category in ['struct', 'union']:
        if stars == 0:
            if brackets == 0:
                return ParamTypeKind.STRUCT
            if brackets == 1:
                return ParamTypeKind.STRUCT_STATIC_ARRAY_1D
            if brackets == 2:
                return ParamTypeKind.STRUCT_STATIC_ARRAY_2D
        if stars == 1:
            if length != '':
                return ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D
            return ParamTypeKind.STRUCT_PTR
        if stars == 2:
            return ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D

    return None


def genParamSerializeToString(param, registry, indent, paramAccess):
    out = ''

    paramName = safeStr(param.find('name').text).strip()
    paramType = safeStr(param.find('type').text).strip()
    paramTypeKind = getParamTypeKind(param, registry)

    customSerializer = ''
    paramTypeElem = getNonAliasTypeElem(paramType, registry)
    paramTypeName = safeStr(paramTypeElem.get('name'))
    paramTypeCategory = safeStr(paramTypeElem.get('category'))
    paramTypeRequires = safeStr(paramTypeElem.get('requires'))
    if paramTypeCategory == 'bitmask' and paramTypeRequires != '':
        customSerializer = paramTypeName

    if 'FlagBits' in paramTypeName:
        customSerializer = paramTypeName.replace('FlagBits', 'Flags')

    if DEBUG_PARAM_TYPE_KIND:
        out += indent + f'// Kind: {paramTypeKind.name}\n'

    match paramTypeKind:
        case (ParamTypeKind.PRIMITIVE |
              ParamTypeKind.HANDLE |
              ParamTypeKind.STRUCT |
              ParamTypeKind.STRING |
              ParamTypeKind.WSTRING |
              ParamTypeKind.VOID_PTR |
              ParamTypeKind.EXTERNAL_PTR |
              ParamTypeKind.FUNCTION_PTR):

            cast = ''
            if paramTypeKind == ParamTypeKind.FUNCTION_PTR:
                cast = '(void*)'

            out += indent + f'SerializeToString{customSerializer}({cast}{paramAccess}{paramName}, stream);\n'

        case (ParamTypeKind.PRIMITIVE_PTR |
              ParamTypeKind.HANDLE_PTR |
              ParamTypeKind.STRUCT_PTR |
              ParamTypeKind.VOID_PTR_PTR |
              ParamTypeKind.EXTERNAL_PTR_PTR):

            out += indent + f'if ({paramAccess}{paramName}) {{\n'
            out += 2 * indent + f'stream << \"{{[\";\n'
            out += 2 * indent + f'SerializeToString{customSerializer}(*{paramAccess}{paramName}, stream);\n'
            out += 2 * indent + f'stream << \"], 1}}\";\n'
            out += indent + f'}}\n'
            out += indent + f'else {{\n'
            out += 2 * indent + f'SerializeToString(nullptr, stream);\n'
            out += indent + f'}}\n'

        case (ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_1D |
              ParamTypeKind.STRUCT_STATIC_ARRAY_1D):

            if paramType == 'char':
                out += indent + f'SerializeToString{customSerializer}({paramAccess}{paramName}, stream);\n'
            else:
                brackets = safeStr(param.find('name').tail).strip()
                for e in param.findall('enum'):
                    brackets += e.text + e.tail

                length = brackets[1:-1]

                out += indent + f'stream << \"{{[\";\n'
                out += indent + f'for (int i = 0; i < {length}; ++i) {{\n'
                out += 2 * indent + f'if (i > 0) {{ stream << \", \"; }}\n'
                out += 2 * indent + f'SerializeToString{customSerializer}({paramAccess}{paramName}[i], stream);\n'
                out += indent + f'}}\n'
                out += indent + f'stream << \"], \" << {length} << \"}}\";\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRING_DYNAMIC_ARRAY_1D |
              ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D):

            length = safeStr(param.get('altlen')).strip()
            if length == '':
                length = safeStr(param.get('len')).strip()

            if paramTypeKind == ParamTypeKind.STRING_DYNAMIC_ARRAY_1D:
                commaPos = length.find(',')
                length = length[:commaPos]

            length, ptrCond = patchDynamicArrayLengthMember(length, paramAccess)
            arrayName = f'{paramAccess}{paramName}'

            out += indent + f'if ({paramAccess}{paramName}{ptrCond}) {{\n'
            out += 2 * indent + f'stream << \"{{[\";\n'
            if paramType == 'void' and paramTypeKind != ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D:
                out += 2 * indent + f'const uint8_t* ptr = reinterpret_cast<const uint8_t*>({paramAccess}{paramName});\n'
                arrayName = 'ptr'

            out += 2 * indent + f'for (int i = 0; i < {length}; ++i) {{\n'
            out += 3 * indent +     f'if (i > 0) {{ stream << \", \"; }}\n'
            out += 3 * indent +     f'SerializeToString{customSerializer}({arrayName}[i], stream);\n'
            out += 2 * indent + f'}}\n'
            out += 2 * indent + f'stream << \"], \" << {length} << \"}}\";\n'
            out += indent + f'}}\n'
            out += indent + f'else {{\n'
            out += 2 * indent + f'SerializeToString(nullptr, stream);\n'
            out += indent + f'}}\n'

        case (ParamTypeKind.PRIMITIVE_STATIC_ARRAY_2D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_2D |
              ParamTypeKind.STRUCT_STATIC_ARRAY_2D):

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            lengths = brackets[1:-1]
            rBracketPos = lengths.find(']')
            lBracketPos = lengths.find('[')
            lengthOuter = lengths[:rBracketPos]
            lengthInner = lengths[lBracketPos + 1:]

            out += indent + f'stream << \"{{[\";\n'
            out += indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 2 * indent + f'if (i > 0) {{ stream << \", \"; }}\n'
            out += 2 * indent + f'stream << \"{{[\";\n'
            out += 2 * indent + f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
            out += 3 * indent +     f'if (j > 0) {{ stream << \", \"; }}\n'
            out += 3 * indent +     f'SerializeToString{customSerializer}({paramAccess}{paramName}[i][j], stream);\n'
            out += 2 * indent + f'}}\n'
            out += 2 * indent + f'stream << \"], \" << {lengthInner} << \"}}\";\n'
            out += indent + f'}}\n'
            out += indent + f'stream << \"], \" << {lengthOuter} << \"}}\";\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D):

            lengths = safeStr(param.get('altlen')).strip()
            if lengths == '':
                lengths = safeStr(param.get('len')).strip()

            commaPos = lengths.find(',')
            lengthOuter = ''
            lengthInner = ''
            ptrCondOuter = ''
            ptrCondInner = ''
            if commaPos != -1:
                lengthOuter = lengths[:commaPos]
                lengthInner = lengths[commaPos + 1:]
                lengthOuter, ptrCondOuter = patchDynamicArrayLengthMember(lengthOuter, paramAccess)
                lengthInner, ptrCondInner = patchDynamicArrayLengthMember(lengthInner, paramAccess)
            else:
                lengthOuter = lengths
                lengthOuter, ptrCondOuter = patchDynamicArrayLengthMember(lengthOuter, paramAccess)
                lengthInner = f'{paramAccess}pInfos[i].geometryCount'
                ptrCondInner = f' && {paramAccess}pInfos'

            out += indent + f'if ({paramAccess}{paramName}{ptrCondOuter}) {{\n'
            out += 2 * indent + f'stream << \"{{[\";\n'
            out += 2 * indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 3 * indent +     f'if (i > 0) {{ stream << \", \"; }}\n'
            out += 3 * indent +     f'if ({paramAccess}{paramName}[i]{ptrCondInner}) {{\n'
            out += 4 * indent +         f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
            out += 5 * indent +             f'if (j > 0) {{ stream << \", \"; }}\n'
            out += 5 * indent +             f'SerializeToString{customSerializer}({paramAccess}{paramName}[i][j], stream);\n'
            out += 4 * indent +         f'}}\n'
            out += 4 * indent +         f'stream << \"], \" << {lengthInner} << \"}}\";\n'
            out += 3 * indent +     f'}}\n'
            out += 3 * indent +     f'else {{\n'
            out += 4 * indent +         f'SerializeToString(nullptr, stream);\n'
            out += 3 * indent +     f'}}\n'
            out += 2 * indent + f'}}\n'
            out += 2 * indent + f'stream << \"], \" << {lengthOuter} << \"}}\";\n'
            out += indent + f'}}\n'
            out += indent + f'else {{\n'
            out += 2 * indent + f'SerializeToString(nullptr, stream);\n'
            out += indent + f'}}\n'

        case ParamTypeKind.PNEXT:

            out += indent + f'SerializeToStringPNext({paramAccess}{paramName}, stream);\n'

    return out


def genParamSerializeToStream(param, registry, indent, paramAccess):
    out = ''

    paramName = safeStr(param.find('name').text).strip()
    paramType = safeStr(param.find('type').text).strip()
    paramTypeKind = getParamTypeKind(param, registry)

    if DEBUG_PARAM_TYPE_KIND:
        out += indent + f'// Kind: {paramTypeKind.name}\n'

    match paramTypeKind:
        case (ParamTypeKind.PRIMITIVE |
              ParamTypeKind.HANDLE |
              ParamTypeKind.STRUCT |
              ParamTypeKind.STRING |
              ParamTypeKind.WSTRING |
              ParamTypeKind.VOID_PTR |
              ParamTypeKind.EXTERNAL_PTR |
              ParamTypeKind.FUNCTION_PTR):

            out += indent + f'SerializeToStream({paramAccess}{paramName}, stream);\n'

        case (ParamTypeKind.PRIMITIVE_PTR |
              ParamTypeKind.HANDLE_PTR |
              ParamTypeKind.STRUCT_PTR |
              ParamTypeKind.VOID_PTR_PTR |
              ParamTypeKind.EXTERNAL_PTR_PTR):

            out += indent + f'if ({paramAccess}{paramName}) {{\n'
            out += 2 * indent + f'SerializeToStream(uint64_t(1), stream);\n'
            out += 2 * indent + f'SerializeToStream(*{paramAccess}{paramName}, stream);\n'
            out += indent + f'}}\n'
            out += indent + f'else {{\n'
            out += 2 * indent + f'SerializeToStream(uint64_t(0), stream);\n'
            out += indent + f'}}\n'

        case (ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_1D |
              ParamTypeKind.PRIMITIVE_STATIC_ARRAY_2D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_2D):

            out += indent + f'SerializeToStream({paramAccess}{paramName}, sizeof({paramAccess}{paramName}), stream);\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRING_DYNAMIC_ARRAY_1D |
              ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D):

            length = safeStr(param.get('altlen')).strip()
            if length == '':
                length = safeStr(param.get('len')).strip()

            if paramTypeKind == ParamTypeKind.STRING_DYNAMIC_ARRAY_1D:
                commaPos = length.find(',')
                length = length[:commaPos]

            length, ptrCond = patchDynamicArrayLengthMember(length, paramAccess)

            out += indent + f'if ({paramAccess}{paramName}{ptrCond}) {{\n'
            out += 2 * indent + f'SerializeToStream(uint64_t({length}), stream);\n'

            if paramTypeKind in [ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D, ParamTypeKind.STRING_DYNAMIC_ARRAY_1D]:
                out += 2 * indent + f'for (int i = 0; i < {length}; ++i) {{\n'
                out += 3 * indent +     f'SerializeToStream({paramAccess}{paramName}[i], stream);\n'
                out += 2 * indent + f'}}\n'
            else:
                size = f'{length}'
                if paramTypeKind == ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D:
                    size += f' * sizeof(void*)'
                elif paramType != 'void':
                    size += f' * sizeof({paramType})'

                out += 2 * indent + f'SerializeToStream({paramAccess}{paramName}, {size}, stream);\n'

            out += indent + f'}}\n'
            out += indent + f'else {{\n'
            out += 2 * indent + f'SerializeToStream(uint64_t(0), stream);\n'
            out += indent + f'}}\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D):

            lengths = safeStr(param.get('altlen')).strip()
            if lengths == '':
                lengths = safeStr(param.get('len')).strip()

            commaPos = lengths.find(',')
            lengthOuter = ''
            lengthInner = ''
            ptrCondOuter = ''
            ptrCondInner = ''
            if commaPos != -1:
                lengthOuter = lengths[:commaPos]
                lengthInner = lengths[commaPos + 1:]
                lengthOuter, ptrCondOuter = patchDynamicArrayLengthMember(lengthOuter, paramAccess)
                lengthInner, ptrCondInner = patchDynamicArrayLengthMember(lengthInner, paramAccess)
            else:
                lengthOuter = lengths
                lengthOuter, ptrCondOuter = patchDynamicArrayLengthMember(lengthOuter, paramAccess)
                lengthInner = f'{paramAccess}pInfos[i].geometryCount'
                ptrCondInner = f' && {paramAccess}pInfos'

            out += indent + f'if ({paramAccess}{paramName}{ptrCondOuter}) {{\n'
            out += 2 * indent + f'SerializeToStream(uint64_t({lengthOuter}), stream);\n'
            out += 2 * indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 3 * indent +     f'if ({paramAccess}{paramName}[i]{ptrCondInner}) {{\n'
            out += 4 * indent +         f'SerializeToStream(uint64_t({lengthInner}), stream);\n'

            if paramTypeKind in [ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D]:
                out += 4 * indent +     f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
                out += 5 * indent +         f'SerializeToStream({paramAccess}{paramName}[i][j], stream);\n'
                out += 4 * indent +     f'}}\n'
            else:
                out += 4 * indent +     f'SerializeToStream({paramAccess}{paramName}[i], {lengthInner} * sizeof({paramType}), stream);\n'

            out += 3 * indent +     f'}}\n'
            out += 3 * indent +     f'else {{\n'
            out += 4 * indent +         f'SerializeToStream(uint64_t(0), stream);\n'
            out += 3 * indent +     f'}}\n'
            out += 2 * indent + f'}}\n'
            out += indent + f'}}\n'
            out += indent + f'else {{\n'
            out += 2 * indent + f'SerializeToStream(uint64_t(0), stream);\n'
            out += indent + f'}}\n'

        case ParamTypeKind.PNEXT:

            out += indent + f'SerializeToStreamPNext({paramAccess}{paramName}, stream);\n'

        case ParamTypeKind.STRUCT_STATIC_ARRAY_1D:

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            length = brackets[1:-1]

            out += indent + f'for (int i = 0; i < {length}; ++i) {{\n'
            out += 2 * indent + f'SerializeToStream({paramAccess}{paramName}[i], stream);\n'
            out += indent + f'}}\n'

        case ParamTypeKind.STRUCT_STATIC_ARRAY_2D:

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            lengths = brackets[1:-1]
            rBracketPos = lengths.find(']')
            lBracketPos = lengths.find('[')
            lengthOuter = lengths[:rBracketPos]
            lengthInner = lengths[lBracketPos + 1:]

            out += indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 2 * indent + f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
            out += 3 * indent +     f'SerializeToStream({paramAccess}{paramName}[i][j], stream);\n'
            out += 2 * indent + f'}}\n'
            out += indent + f'}}\n'

    return out


def genParamDeserializeFromStream(param, registry, indent, paramAccess):
    out = ''

    paramName = safeStr(param.find('name').text).strip()
    paramType = safeStr(param.find('type').text).strip()
    paramTypeKind = getParamTypeKind(param, registry)

    paramFullType = safeStr(param.text).strip() + ' ' + safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()

    paramFullStrippedType = paramFullType
    paramFullStrippedType = paramFullStrippedType.replace('const', '')
    paramFullStrippedType = paramFullStrippedType.replace('volatile', '')
    paramFullStrippedType = paramFullStrippedType.replace('struct', '')
    paramFullStrippedType = paramFullStrippedType.replace('union', '')
    paramFullStrippedType = paramFullStrippedType.strip()

    if DEBUG_PARAM_TYPE_KIND:
        out += indent + f'// Kind: {paramTypeKind.name}\n'

    match paramTypeKind:
        case (ParamTypeKind.PRIMITIVE |
              ParamTypeKind.HANDLE |
              ParamTypeKind.STRUCT |
              ParamTypeKind.STRING |
              ParamTypeKind.WSTRING |
              ParamTypeKind.VOID_PTR |
              ParamTypeKind.EXTERNAL_PTR |
              ParamTypeKind.FUNCTION_PTR):

            tail = safeStr(param.find('name').tail).strip()
            if ':' in tail:
                out += indent + f'{paramType} {paramName}Bitfield = 0;\n'
                out += indent + f'DeserializeFromStream({paramName}Bitfield, allocator, stream);\n'
                out += indent + f'{paramAccess}{paramName} = {paramName}Bitfield;\n'
            else:
                cast = ''
                if 'const' in paramFullType:
                    cast = f'({paramFullStrippedType}&)'
                if paramTypeKind == ParamTypeKind.WSTRING:
                    cast = '(LPWSTR&)'

                out += indent + f'DeserializeFromStream({cast}{paramAccess}{paramName}, allocator, stream);\n'

        case (ParamTypeKind.PRIMITIVE_PTR |
              ParamTypeKind.HANDLE_PTR |
              ParamTypeKind.STRUCT_PTR |
              ParamTypeKind.VOID_PTR_PTR |
              ParamTypeKind.EXTERNAL_PTR_PTR):

            allocType = paramFullStrippedType[:-1].strip()

            out += indent + f'{allocType}* {paramName}Ptr = nullptr;\n'
            out += indent + f'uint64_t {paramName}Size = 0;\n'
            out += indent + f'DeserializeFromStream({paramName}Size, allocator, stream);\n'
            out += indent + f'if ({paramName}Size != 0) {{\n'
            out += 2 * indent + f'{paramName}Ptr = allocator.Allocate<{allocType}>();\n'
            out += 2 * indent + f'DeserializeFromStream(*{paramName}Ptr, allocator, stream);\n'
            out += indent + f'}}\n'
            out += indent + f'{paramAccess}{paramName} = {paramName}Ptr;\n'

        case (ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_1D |
              ParamTypeKind.PRIMITIVE_STATIC_ARRAY_2D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_2D):

            out += indent + f'DeserializeFromStream((void*){paramAccess}{paramName}, sizeof({paramAccess}{paramName}), allocator, stream);\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRING_DYNAMIC_ARRAY_1D |
              ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D):

            allocType = paramFullStrippedType[:-1].strip()
            if allocType == 'void':
                allocType = 'uint8_t'

            out += indent + f'{allocType}* {paramName}Ptr = nullptr;\n'
            out += indent + f'uint64_t {paramName}Size = 0;\n'
            out += indent + f'DeserializeFromStream({paramName}Size, allocator, stream);\n'
            out += indent + f'if ({paramName}Size != 0) {{\n'
            out += 2 * indent + f'{paramName}Ptr = allocator.Allocate<{allocType}>({paramName}Size);\n'

            if paramTypeKind in [ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D, ParamTypeKind.STRING_DYNAMIC_ARRAY_1D]:
                out += 2 * indent + f'for (int i = 0; i < {paramName}Size; ++i) {{\n'
                out += 3 * indent +     f'DeserializeFromStream({paramName}Ptr[i], allocator, stream);\n'
                out += 2 * indent + f'}}\n'
            else:
                out += 2 * indent + f'DeserializeFromStream((void*){paramName}Ptr, {paramName}Size * sizeof({allocType}), allocator, stream);\n'

            out += indent + f'}}\n'
            out += indent + f'{paramAccess}{paramName} = {paramName}Ptr;\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D):

            allocType = paramType

            out += indent + f'{allocType}** {paramName}Ptr = nullptr;\n'
            out += indent + f'uint64_t {paramName}SizeOuter = 0;\n'
            out += indent + f'DeserializeFromStream({paramName}SizeOuter, allocator, stream);\n'
            out += indent + f'if ({paramName}SizeOuter != 0) {{\n'
            out += 2 * indent + f'{paramName}Ptr = allocator.Allocate<{allocType}*>({paramName}SizeOuter);\n'
            out += 2 * indent + f'for (int i = 0; i < {paramName}SizeOuter; ++i) {{\n'
            out += 3 * indent +     f'{paramName}Ptr[i] = nullptr;\n'
            out += 3 * indent +     f'uint64_t {paramName}SizeInner = 0;\n'
            out += 3 * indent +     f'DeserializeFromStream({paramName}SizeInner, allocator, stream);\n'
            out += 3 * indent +     f'if ({paramName}SizeInner != 0) {{\n'
            out += 4 * indent +         f'{paramName}Ptr[i] = allocator.Allocate<{allocType}>({paramName}SizeInner);\n'

            if paramTypeKind in [ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D]:
                out += 4 * indent +     f'for (int j = 0; j < {paramName}SizeInner; ++j) {{\n'
                out += 5 * indent +         f'DeserializeFromStream({paramName}Ptr[i][j], allocator, stream);\n'
                out += 4 * indent +     f'}}\n'
            else:
                out += 4 * indent +     f'DeserializeFromStream((void*){paramName}Ptr[i], {paramName}SizeInner * sizeof({paramType}), allocator, stream);\n'

            out += 3 * indent +     f'}}\n'
            out += 2 * indent + f'}}\n'
            out += indent + f'}}\n'
            out += indent + f'{paramAccess}{paramName} = {paramName}Ptr;\n'

        case ParamTypeKind.PNEXT:

            cast = ''
            if paramType != 'void' or 'const' in paramFullType:
                cast = '(void**)'

            out += indent + f'DeserializeFromStreamPNext({cast}&{paramAccess}{paramName}, allocator, stream);\n'

        case ParamTypeKind.STRUCT_STATIC_ARRAY_1D:

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            length = brackets[1:-1]

            out += indent + f'for (int i = 0; i < {length}; ++i) {{\n'
            out += 2 * indent + f'DeserializeFromStream({paramAccess}{paramName}[i], allocator, stream);\n'
            out += indent + f'}}\n'

        case ParamTypeKind.STRUCT_STATIC_ARRAY_2D:

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            lengths = brackets[1:-1]
            rBracketPos = lengths.find(']')
            lBracketPos = lengths.find('[')
            lengthOuter = lengths[:rBracketPos]
            lengthInner = lengths[lBracketPos + 1:]

            out += indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 2 * indent + f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
            out += 3 * indent +     f'DeserializeFromStream({paramAccess}{paramName}[i][j], allocator, stream);\n'
            out += 2 * indent + f'}}\n'
            out += indent + f'}}\n'

    return out


def genParamDeepCopy(param, registry, indent, paramInAccess, paramOutAccess):
    out = ''

    paramName = safeStr(param.find('name').text).strip()
    paramType = safeStr(param.find('type').text).strip()
    paramTypeKind = getParamTypeKind(param, registry)

    paramFullType = safeStr(param.text).strip() + ' ' + safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()

    paramFullStrippedType = paramFullType
    paramFullStrippedType = paramFullStrippedType.replace('const', '')
    paramFullStrippedType = paramFullStrippedType.replace('volatile', '')
    paramFullStrippedType = paramFullStrippedType.replace('struct', '')
    paramFullStrippedType = paramFullStrippedType.replace('union', '')
    paramFullStrippedType = paramFullStrippedType.strip()

    if DEBUG_PARAM_TYPE_KIND:
        out += indent + f'// Kind: {paramTypeKind.name}\n'

    match paramTypeKind:
        case (ParamTypeKind.PRIMITIVE |
              ParamTypeKind.HANDLE |
              ParamTypeKind.STRUCT |
              ParamTypeKind.STRING |
              ParamTypeKind.WSTRING |
              ParamTypeKind.VOID_PTR |
              ParamTypeKind.EXTERNAL_PTR |
              ParamTypeKind.FUNCTION_PTR):

            tail = safeStr(param.find('name').tail).strip()
            if ':' in tail:
                out += indent + f'{paramType} {paramName}Bitfield = 0;\n'
                out += indent + f'DeepCopy({paramInAccess}{paramName}, allocator, {paramName}Bitfield);\n'
                out += indent + f'{paramOutAccess}{paramName} = {paramName}Bitfield;\n'
            else:
                cast = ''
                if 'const' in paramFullType:
                    cast = f'({paramFullStrippedType}&)'
                if paramTypeKind == ParamTypeKind.WSTRING:
                    cast = '(LPWSTR&)'

                out += indent + f'DeepCopy({cast}{paramInAccess}{paramName}, allocator, {cast}{paramOutAccess}{paramName});\n'

        case (ParamTypeKind.PRIMITIVE_PTR |
              ParamTypeKind.HANDLE_PTR |
              ParamTypeKind.STRUCT_PTR |
              ParamTypeKind.VOID_PTR_PTR |
              ParamTypeKind.EXTERNAL_PTR_PTR):

            allocType = paramFullStrippedType[:-1].strip()

            out += indent + f'{allocType}* {paramName}Ptr = nullptr;\n'
            out += indent + f'if ({paramInAccess}{paramName}) {{\n'
            out += 2 * indent + f'{paramName}Ptr = allocator.Allocate<{allocType}>();\n'
            out += 2 * indent + f'DeepCopy(*{paramInAccess}{paramName}, allocator, *{paramName}Ptr);\n'
            out += indent + f'}}\n'
            out += indent + f'{paramOutAccess}{paramName} = {paramName}Ptr;\n'

        case (ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_1D |
              ParamTypeKind.PRIMITIVE_STATIC_ARRAY_2D |
              ParamTypeKind.HANDLE_STATIC_ARRAY_2D):

            out += indent + f'DeepCopy((void*){paramInAccess}{paramName}, allocator, (void*){paramOutAccess}{paramName}, sizeof({paramOutAccess}{paramName}));\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D |
              ParamTypeKind.STRING_DYNAMIC_ARRAY_1D |
              ParamTypeKind.VOID_PTR_DYNAMIC_ARRAY_1D):

            length = safeStr(param.get('altlen')).strip()
            if length == '':
                length = safeStr(param.get('len')).strip()

            if paramTypeKind == ParamTypeKind.STRING_DYNAMIC_ARRAY_1D:
                commaPos = length.find(',')
                length = length[:commaPos]

            length, ptrCond = patchDynamicArrayLengthMember(length, paramInAccess)

            allocType = paramFullStrippedType[:-1].strip()
            if allocType == 'void':
                allocType = 'uint8_t'

            out += indent + f'{allocType}* {paramName}Ptr = nullptr;\n'
            out += indent + f'if ({paramInAccess}{paramName}{ptrCond}) {{\n'
            out += 2 * indent + f'{paramName}Ptr = allocator.Allocate<{allocType}>({length});\n'

            if paramTypeKind in [ParamTypeKind.STRUCT_DYNAMIC_ARRAY_1D, ParamTypeKind.STRING_DYNAMIC_ARRAY_1D]:
                out += 2 * indent + f'for (int i = 0; i < {length}; ++i) {{\n'
                out += 3 * indent +     f'DeepCopy({paramInAccess}{paramName}[i], allocator, {paramName}Ptr[i]);\n'
                out += 2 * indent + f'}}\n'
            else:
                out += 2 * indent + f'DeepCopy((void*){paramInAccess}{paramName}, allocator, (void*){paramName}Ptr, {length} * sizeof({allocType}));\n'

            out += indent + f'}}\n'
            out += indent + f'{paramOutAccess}{paramName} = {paramName}Ptr;\n'

        case (ParamTypeKind.PRIMITIVE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.HANDLE_DYNAMIC_ARRAY_2D |
              ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D):

            lengths = safeStr(param.get('altlen')).strip()
            if lengths == '':
                lengths = safeStr(param.get('len')).strip()

            commaPos = lengths.find(',')
            lengthOuter = ''
            lengthInner = ''
            ptrCondOuter = ''
            ptrCondInner = ''
            if commaPos != -1:
                lengthOuter = lengths[:commaPos]
                lengthInner = lengths[commaPos + 1:]
                lengthOuter, ptrCondOuter = patchDynamicArrayLengthMember(lengthOuter, paramInAccess)
                lengthInner, ptrCondInner = patchDynamicArrayLengthMember(lengthInner, paramInAccess)
            else:
                lengthOuter = lengths
                lengthOuter, ptrCondOuter = patchDynamicArrayLengthMember(lengthOuter, paramInAccess)
                lengthInner = f'{paramInAccess}pInfos[i].geometryCount'
                ptrCondInner = f' && {paramInAccess}pInfos'

            allocType = paramType

            out += indent + f'{allocType}** {paramName}Ptr = nullptr;\n'
            out += indent + f'if ({paramInAccess}{paramName}{ptrCondOuter}) {{\n'
            out += 2 * indent + f'{paramName}Ptr = allocator.Allocate<{allocType}*>({lengthOuter});\n'
            out += 2 * indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 3 * indent +     f'{paramName}Ptr[i] = nullptr;\n'
            out += 3 * indent +     f'if ({paramInAccess}{paramName}[i]{ptrCondInner}) {{\n'
            out += 4 * indent +         f'{paramName}Ptr[i] = allocator.Allocate<{allocType}>({lengthInner});\n'

            if paramTypeKind in [ParamTypeKind.STRUCT_DYNAMIC_ARRAY_2D]:
                out += 4 * indent +     f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
                out += 5 * indent +         f'DeepCopy({paramInAccess}{paramName}[i][j], allocator, {paramName}Ptr[i][j]);\n'
                out += 4 * indent +     f'}}\n'
            else:
                out += 4 * indent +     f'DeepCopy((void*){paramInAccess}{paramName}[i], allocator, (void*){paramName}Ptr[i], {lengthInner} * sizeof({paramType}));\n'

            out += 3 * indent +     f'}}\n'
            out += 2 * indent + f'}}\n'
            out += indent + f'}}\n'
            out += indent + f'{paramOutAccess}{paramName} = {paramName}Ptr;\n'

        case ParamTypeKind.PNEXT:

            cast = ''
            if paramType != 'void' or 'const' in paramFullType:
                cast = '(void**)'

            out += indent + f'DeepCopyPNext({paramInAccess}{paramName}, allocator, {cast}&{paramOutAccess}{paramName});\n'

        case ParamTypeKind.STRUCT_STATIC_ARRAY_1D:

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            length = brackets[1:-1]

            out += indent + f'for (int i = 0; i < {length}; ++i) {{\n'
            out += 2 * indent + f'DeepCopy({paramInAccess}{paramName}[i], allocator, {paramOutAccess}{paramName}[i]);\n'
            out += indent + f'}}\n'

        case ParamTypeKind.STRUCT_STATIC_ARRAY_2D:

            brackets = safeStr(param.find('name').tail).strip()
            for e in param.findall('enum'):
                brackets += e.text + e.tail

            lengths = brackets[1:-1]
            rBracketPos = lengths.find(']')
            lBracketPos = lengths.find('[')
            lengthOuter = lengths[:rBracketPos]
            lengthInner = lengths[lBracketPos + 1:]

            out += indent + f'for (int i = 0; i < {lengthOuter}; ++i) {{\n'
            out += 2 * indent + f'for (int j = 0; j < {lengthInner}; ++j) {{\n'
            out += 3 * indent +     f'DeepCopy({paramInAccess}{paramName}[i][j], allocator, {paramOutAccess}{paramName}[i][j]);\n'
            out += 2 * indent + f'}}\n'
            out += indent + f'}}\n'

    return out


class COutputGeneratorCustom(COutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.header_guard = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        header = autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        header += f'#ifndef {self.header_guard}\n'
        header += f'#define {self.header_guard}\n\n'

        self.outFile.write(header)

    def endFile(self):
        footer = ''
        footer += f'\n'
        footer += f'#endif // {self.header_guard}'

        self.outFile.write(footer)

        OutputGenerator.endFile(self)

    def beginFeature(self, interface, emit):
        COutputGenerator.beginFeature(self, interface, emit)

    def endFeature(self):
        includes = self.sections['include']
        for i in range(0, len(includes)):
            includes[i] = includes[i].replace('vk_platform.h', 'vulkan/vk_platform.h')
        COutputGenerator.endFeature(self)


class COutputGeneratorVulkanDispatchTables(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.instance_table = ''
        self.device_table = ''
        self.load_instance = ''
        self.load_device = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <Vulkan.h>\n\n'
        self.body += f'namespace OVS {{\n'

        self.instance_table += f'struct VulkanInstanceDispatchTable {{\n'
        self.device_table   += f'struct VulkanDeviceDispatchTable {{\n'

        self.load_instance  += f'static void LoadInstanceDispatchTable(PFN_vkGetInstanceProcAddr gipa, VkInstance instance, VulkanInstanceDispatchTable& dt) {{\n'
        self.load_device    += f'static void LoadDeviceDispatchTable(PFN_vkGetDeviceProcAddr gdpa, VkDevice device, VulkanDeviceDispatchTable& dt) {{\n'

    def endFile(self):
        self.instance_table += f'}};\n'
        self.device_table   += f'}};\n'

        self.load_instance  += f'}};\n'
        self.load_device    += f'}};\n'

        self.body += f'\n'
        self.body += self.instance_table
        self.body += f'\n'
        self.body += self.device_table
        self.body += f'\n'
        self.body += f'struct VulkanDispatchTable : VulkanInstanceDispatchTable, VulkanDeviceDispatchTable {{}};\n'
        self.body += f'\n'
        self.body += f'\n'
        self.body += self.load_instance
        self.body += f'\n'
        self.body += self.load_device
        self.body += f'\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        indent = '    '

        first_param_type = getFirstParamType(cmdinfo)
        if first_param_type is not None:
            if first_param_type == 'VkInstance' or first_param_type == 'VkPhysicalDevice' or name in SPECIAL_INSTANCE_COMMANDS:
                self.instance_table += indent + f'PFN_{name} {name}{{nullptr}};\n'
            else:
                self.device_table   += indent + f'PFN_{name} {name}{{nullptr}};\n'

            if name == 'vkGetInstanceProcAddr':
                self.load_instance += indent + f'dt.{name} = gipa;\n'
            elif name == 'vkGetDeviceProcAddr':
                self.load_device   += indent + f'dt.{name} = gdpa;\n'
            elif name in SPECIAL_INSTANCE_COMMANDS:
                self.load_instance += indent + f'dt.{name} = (PFN_{name})gipa(nullptr, \"{name}\");\n'
            elif first_param_type == 'VkInstance' or first_param_type == 'VkPhysicalDevice':
                self.load_instance += indent + f'dt.{name} = (PFN_{name})gipa(instance, \"{name}\");\n'
            else:
                self.load_device   += indent + f'dt.{name} = (PFN_{name})gdpa(device, \"{name}\");\n'


class COutputGeneratorVulkanLayerEntry(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.func_table = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <Macros.h>\n'
        self.body += f'#include <VulkanLayerManager.h>\n\n'
        self.body += f'#include <string>\n'
        self.body += f'#include <unordered_map>\n\n'
        self.body += f'OVS::VulkanLayerManager sLayerManager;\n\n'
        self.body += f'extern "C" {{\n\n'

        self.func_table = f'std::unordered_map<std::string, PFN_vkVoidFunction> sFunctionTable = {{\n'

    def endFile(self):
        self.func_table += f'}};\n\n'

        self.body += f'}} // extern "C"\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += self.func_table
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        indent = '    '

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        defi = ''

        defi += indent
        if name in SPECIAL_INSTANCE_COMMANDS:
            defi += f'sLayerManager.Init();\n'
            defi += indent
        if type != 'void':
            defi += 'return '
        defi += f'sLayerManager.GetFrontLayer()->{name}('
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            if i > 0:
                decl += f', '
                defi += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()
            defi += f'{param_name}'
        defi += ');'

        self.body += f'OVS_EXPORT VKAPI_ATTR {type} VKAPI_CALL OVS_Debug_{name}({decl}) {{\n'
        self.body += f'{defi}\n'
        self.body += f'}}\n\n'

        self.func_table += indent + f'{{\"{name}\", (PFN_vkVoidFunction)OVS_Debug_{name}}},\n'


class COutputGeneratorVulkanLayerInterface(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <Vulkan.h>\n'
        self.body += f'#include <VulkanLayer.h>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'class VulkanLayerInterface {{\n'
        self.body += f'public:\n'

    def endFile(self):
        self.body += f'\n'
        self.body += self.indent + f'inline VulkanLayerType GetType() const {{ return type_; }}\n\n'
        self.body += self.indent + f'inline VulkanLayerInterface* GetNext() const {{ return next_; }}\n'
        self.body += self.indent + f'inline void SetNext(VulkanLayerInterface* next) {{ next_ = next; }}\n\n'
        self.body += self.indent + f'explicit VulkanLayerInterface(VulkanLayerType type) : type_{{type}} {{}}\n'
        self.body += self.indent + f'virtual ~VulkanLayerInterface() {{}}\n\n'
        self.body += f'protected:\n'
        self.body += self.indent + f'VulkanLayerType type_{{VulkanLayerType::None}};\n'
        self.body += self.indent + f'VulkanLayerInterface* next_{{nullptr}};\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            if i > 0:
                decl += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()

        self.body += self.indent + f'virtual {type} {name}({decl}) = 0;\n'


class COutputGeneratorVulkanLayerTerminatorBase(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <VulkanLayerInterfaceGenerated.h>\n'
        self.body += f'#include <VulkanDispatchTablesGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'class VulkanLayerTerminatorBase : public VulkanLayerInterface {{\n'
        self.body += f'public:\n'

    def endFile(self):
        self.body += self.indent + f'VulkanLayerTerminatorBase() : VulkanLayerInterface(VulkanLayerType::TerminatorBase) {{}}\n'
        self.body += self.indent + f'virtual ~VulkanLayerTerminatorBase() {{}}\n\n'
        self.body += f'protected:\n'
        self.body += self.indent + f'explicit VulkanLayerTerminatorBase(VulkanLayerType type) : VulkanLayerInterface(type) {{}}\n\n'
        self.body += self.indent + f'VulkanDispatchTable dispatchTableNative_;\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        defi = ''

        defi += 2 * self.indent
        if type != 'void':
            defi += 'return '
        defi += f'dispatchTableNative_.{name}('
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            if i > 0:
                decl += f', '
                defi += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()
            defi += f'{param_name}'
        defi += ');'

        self.body += self.indent + f'virtual {type} {name}({decl}) override {{\n'
        self.body += f'{defi}\n'
        self.body += self.indent + f'}}\n\n'


class COutputGeneratorVulkanLayerPrinter(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <VulkanLayerInterfaceGenerated.h>\n\n'
        self.body += f'#include <fstream>\n'
        self.body += f'#include <iostream>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'class VulkanLayerPrinter : public VulkanLayerInterface {{\n'
        self.body += f'public:\n'

    def endFile(self):
        self.body += self.indent + f'explicit VulkanLayerPrinter(const VulkanLayerPrinterSettings& settings) : VulkanLayerInterface(VulkanLayerType::Printer), settings_{{settings}} {{\n'
        self.body += 2 * self.indent + f'std::streambuf* buf = std::cout.rdbuf();\n'
        self.body += 2 * self.indent + f'if (settings_.filename != \"stdout\") {{\n'
        self.body += 3 * self.indent +     f'fout_ = std::ofstream(settings_.filename);\n'
        self.body += 3 * self.indent +     f'buf = fout_.rdbuf();\n'
        self.body += 2 * self.indent + f'}}\n'
        self.body += 2 * self.indent + f'out_.set_rdbuf(buf);\n'
        self.body += self.indent + f'}}\n\n'
        self.body += self.indent + f'virtual ~VulkanLayerPrinter() {{}}\n\n'
        self.body += f'private:\n'
        self.body += self.indent + f'VulkanLayerPrinterSettings settings_{{}};\n'
        self.body += self.indent + f'std::ofstream fout_;\n'
        self.body += self.indent + f'std::ostream out_{{std::cout.rdbuf()}};\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        defi = ''

        defi += 2 * self.indent + f'out_ << \"{name}\" << \'\\n\';\n'
        defi += 2 * self.indent
        if type != 'void':
            defi += 'return '
        defi += f'next_->{name}('
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            if i > 0:
                decl += f', '
                defi += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()
            defi += f'{param_name}'
        defi += ');'

        self.body += self.indent + f'virtual {type} {name}({decl}) override {{\n'
        self.body += f'{defi}\n'
        self.body += self.indent + f'}}\n\n'


class COutputGeneratorVulkanLayerPassThrough(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <VulkanLayerInterfaceGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'class VulkanLayerPassThrough : public VulkanLayerInterface {{\n'
        self.body += f'public:\n'

    def endFile(self):
        self.body += self.indent + f'VulkanLayerPassThrough() : VulkanLayerInterface(VulkanLayerType::PassThrough) {{}}\n'
        self.body += self.indent + f'virtual ~VulkanLayerPassThrough() {{}}\n\n'
        self.body += f'protected:\n'
        self.body += self.indent + f'explicit VulkanLayerPassThrough(VulkanLayerType type) : VulkanLayerInterface(type) {{}}\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        defi = ''

        defi += 2 * self.indent
        if type != 'void':
            defi += 'return '
        defi += f'next_->{name}('
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            if i > 0:
                decl += f', '
                defi += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()
            defi += f'{param_name}'
        defi += ');'

        self.body += self.indent + f'virtual {type} {name}({decl}) override {{\n'
        self.body += f'{defi}\n'
        self.body += self.indent + f'}}\n\n'


class COutputGeneratorVulkanAPICallID(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.call_id_enum = ''
        self.call_id_name = ''
        self.indent = '    '
        self.call_id = 0

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <cstdint>\n\n'
        self.body += f'namespace OVS {{\n'

        self.call_id_enum += f'enum class APICallID : uint16_t {{\n'
        self.call_id_enum += self.indent + f'Unknown = {self.call_id},\n'

        self.call_id_name += f'static const char* GetAPICallName(APICallID id) {{\n'
        self.call_id_name += self.indent + f'switch (id) {{\n'
        self.call_id_name += 2 * self.indent + f'default: return \"Unknown\";\n'

        self.call_id += 1

    def endFile(self):
        self.call_id_enum += f'\n'
        self.call_id_enum += self.indent + f'Count\n'
        self.call_id_enum += f'}};\n'

        self.call_id_name += self.indent + f'}};\n'
        self.call_id_name += f'}};\n'

        self.body += f'\n'
        self.body += self.call_id_enum
        self.body += f'\n'
        self.body += self.call_id_name
        self.body += f'\n'

        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        self.call_id_enum += self.indent + f'{name} = {self.call_id},\n'

        self.call_id_name += 2 * self.indent + f'case APICallID::{name}: return \"{name}\";\n'

        self.call_id += 1


class COutputGeneratorVulkanAPIStructID(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '
        self.struct_id = 0

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <cstdint>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'enum class APIStructID : uint16_t {{\n'
        self.body += self.indent + f'Unknown = {self.struct_id},\n'
        self.struct_id += 1

    def endFile(self):
        self.body += f'\n'
        self.body += self.indent + f'Count\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        self.body += self.indent + f'{name} = {self.struct_id},\n'
        self.struct_id += 1


class COutputGeneratorSignatureDeclaration(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '
        self.sig_forward_decl = ''
        self.sig_decl = ''
        self.sig_create = ''
        self.ser_to_str = ''
        self.ser_to_json = ''
        self.ser_to_stream = ''
        self.deser_from_stream = ''
        self.deser_from_json = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <Vulkan.h>\n'
        self.body += f'#include <SignatureUtils.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

        self.sig_create += f'static SignaturePtr CreateSignature(APICallID callID) {{\n'
        self.sig_create += self.indent + f'switch (callID) {{\n'

    def endFile(self):
        self.sig_create += 2 * self.indent + f'default: return nullptr;\n'
        self.sig_create += self.indent + f'}}\n'
        self.sig_create += f'}}\n'

        self.body += self.sig_forward_decl
        self.body += f'\n'
        self.body += self.ser_to_str
        self.body += f'\n'
        self.body += self.ser_to_json
        self.body += f'\n'
        self.body += self.ser_to_stream
        self.body += f'\n'
        self.body += self.deser_from_stream
        self.body += f'\n'
        self.body += self.deser_from_json
        self.body += f'\n'
        self.body += self.sig_decl
        self.body += f'\n'
        self.body += self.sig_create
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        self.sig_decl += f'struct {name}Signature : public BaseSignature {{\n'
        if type != 'void':
            retParamTypeKind = None
            typeElem = getNonAliasTypeElem(type, self.registry)
            category = safeStr(typeElem.get('category'))
            if category == 'funcpointer':
                retParamTypeKind = ParamTypeKind.FUNCTION_PTR
            if type in BASE_TYPES or category in ['basetype', 'enum', 'bitmask']:
                retParamTypeKind = ParamTypeKind.PRIMITIVE

            if DEBUG_PARAM_TYPE_KIND:
                self.sig_decl += self.indent + f'// Kind: {retParamTypeKind.name}\n'

            self.sig_decl += self.indent + f'{type} ret{{}};\n'

        for i in range(len(params)):
            param = params[i]
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            paramTypeKind = getParamTypeKind(param, self.registry)

            if DEBUG_PARAM_TYPE_KIND:
                self.sig_decl += self.indent + f'// Kind: {paramTypeKind.name}\n'

            self.sig_decl += self.indent + f'{param_type} {param_name}{param_tail}{{}};\n'

        self.sig_decl += f'\n'
        self.sig_decl += self.indent + f'virtual void SerializeToString(std::stringstream& stream) const {{ SignatureSerializer::SerializeToString(*this, stream); }}\n'
        self.sig_decl += self.indent + f'virtual void SerializeToJSON(std::stringstream& stream) const {{ /*TODO*/ }}\n'
        self.sig_decl += self.indent + f'virtual void SerializeToStream(WriteStream& stream) const {{ SignatureSerializer::SerializeToStream(*this, stream); }}\n'
        self.sig_decl += self.indent + f'virtual void DeserializeFromStream(const ReadStream& stream) {{ SignatureSerializer::DeserializeFromStream(*this, stream); }}\n'
        self.sig_decl += self.indent + f'virtual void DeserializeFromJSON(const std::string& json) {{ /*TODO*/ }}\n'
        self.sig_decl += self.indent + f'virtual ~{name}Signature() {{}}\n'
        self.sig_decl += f'}};\n\n'

        self.sig_forward_decl   += f'struct {name}Signature;\n'
        self.sig_create         += 2 * self.indent + f'case APICallID::{name}: return std::make_unique<{name}Signature>();\n'
        self.ser_to_str         += f'void SerializeToString(const {name}Signature& sig, std::stringstream& stream);\n'
        self.ser_to_json        += f'void SerializeToJSON(const {name}Signature& sig, std::stringstream& stream);\n'
        self.ser_to_stream      += f'void SerializeToStream(const {name}Signature& sig, WriteStream& stream);\n'
        self.deser_from_stream  += f'void DeserializeFromStream({name}Signature& sig, const ReadStream& stream);\n'
        self.deser_from_json    += f'void DeserializeFromJSON({name}Signature& sig, const std::string& json);\n'


class COutputGeneratorStructSerializeDeclaration(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '
        self.ser_to_str = ''
        self.ser_to_json = ''
        self.ser_to_stream = ''
        self.deser_from_stream = ''
        self.deser_from_json = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <Vulkan.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.ser_to_str         += f'void SerializeToStringPNext(const void* pNext, std::stringstream& stream);\n'
        self.ser_to_json        += f'void SerializeToJSONPNext(const void* pNext, std::stringstream& stream);\n'
        self.ser_to_stream      += f'void SerializeToStreamPNext(const void* pNext, WriteStream& stream);\n'
        self.deser_from_stream  += f'void DeserializeFromStreamPNext(void** ppNext, Allocator& allocator, const ReadStream& stream);\n'
        self.deser_from_json    += f'void DeserializeFromJSONPNext(void** ppNext, Allocator& allocator, const std::string& json);\n'

        self.body += self.ser_to_str
        self.body += f'\n'
        self.body += self.ser_to_json
        self.body += f'\n'
        self.body += self.ser_to_stream
        self.body += f'\n'
        self.body += self.deser_from_stream
        self.body += f'\n'
        self.body += self.deser_from_json
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        self.ser_to_str         += f'void SerializeToString(const {name}& value, std::stringstream& stream);\n'
        self.ser_to_json        += f'void SerializeToJSON(const {name}& value, std::stringstream& stream);\n'
        self.ser_to_stream      += f'void SerializeToStream(const {name}& value, WriteStream& stream);\n'
        self.deser_from_stream  += f'void DeserializeFromStream({name}& value, Allocator& allocator, const ReadStream& stream);\n'
        self.deser_from_json    += f'void DeserializeFromJSON({name}& value, Allocator& allocator, const std::string& json);\n'


class COutputGeneratorStructSerializeToString(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '
        self.ser_to_str = ''
        self.ser_pnext = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <StructSerializeGenerated.h>\n'
        self.body += f'#include <EnumSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

        self.ser_pnext += f'void SerializeToStringPNext(const void* pNext, std::stringstream& stream) {{\n'
        self.ser_pnext += self.indent + f'if (!pNext) {{\n'
        self.ser_pnext += 2 * self.indent + f'SerializeToString(pNext, stream);\n'
        self.ser_pnext += 2 * self.indent + f'return;\n'
        self.ser_pnext += self.indent + f'}}\n\n'
        self.ser_pnext += self.indent + f'const VkBaseInStructure* pNextStruct = reinterpret_cast<const VkBaseInStructure*>(pNext);\n'
        self.ser_pnext += self.indent + f'switch (pNextStruct->sType) {{\n'

    def endFile(self):
        self.ser_pnext += 2 * self.indent + f'default: {{\n'
        self.ser_pnext += 3 * self.indent +     f'SerializeToString(*pNextStruct, stream);\n'
        self.ser_pnext += 3 * self.indent +     f'break;\n'
        self.ser_pnext += 2 * self.indent + f'}}\n'
        self.ser_pnext += self.indent + f'}}\n'
        self.ser_pnext += f'}}\n'

        self.body += self.ser_to_str
        self.body += f'\n'
        self.body += self.ser_pnext
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        members = typeinfo.elem.findall('member')

        self.ser_to_str += f'void SerializeToString(const {name}& value, std::stringstream& stream) {{\n'
        self.ser_to_str += self.indent + f'stream << \"{name}{{\";\n'

        memberAccess = 'value.'
        for i in range(len(members)):
            member = members[i]
            memberName = safeStr(member.find('name').text).strip()

            if i > 0:
                self.ser_to_str += self.indent + f'stream << \", \";\n'

            self.ser_to_str += genParamSerializeToString(member, self.registry, self.indent, memberAccess)
            if memberName == 'sType':
                sTypeValue = member.get('values')
                if sTypeValue is not None:
                    enumProtect = None
                    enumInfo = self.registry.lookupElementInfo(sTypeValue, self.registry.enumdict)
                    if enumInfo is not None:
                        enumElem = enumInfo.elem
                        enumProtect = enumElem.get('protect')

                    if enumProtect:
                        self.ser_pnext += f'#ifdef {enumProtect}\n'

                    self.ser_pnext += 2 * self.indent + f'case {sTypeValue}: {{\n'
                    self.ser_pnext += 3 * self.indent +     f'const {name}* pValue = reinterpret_cast<const {name}*>(pNext);\n'
                    self.ser_pnext += 3 * self.indent +     f'SerializeToString(*pValue, stream);\n'
                    self.ser_pnext += 3 * self.indent +     f'break;\n'
                    self.ser_pnext += 2 * self.indent + f'}}\n'

                    if enumProtect:
                        self.ser_pnext += f'#endif\n'

        self.ser_to_str += self.indent + f'stream << \"}}\";\n'
        self.ser_to_str += f'}}\n\n'


class COutputGeneratorStructSerializeToStream(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '
        self.ser = ''
        self.ser_pnext = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <StructSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

        self.ser_pnext += f'void SerializeToStreamPNext(const void* pNext, WriteStream& stream) {{\n'
        self.ser_pnext += self.indent + f'if (!pNext) {{\n'
        self.ser_pnext += 2 * self.indent + f'SerializeToStream(uint64_t(0), stream);\n'
        self.ser_pnext += 2 * self.indent + f'return;\n'
        self.ser_pnext += self.indent + f'}}\n\n'
        self.ser_pnext += self.indent + f'SerializeToStream(uint64_t(1), stream);\n\n'
        self.ser_pnext += self.indent + f'const VkBaseInStructure* pNextStruct = reinterpret_cast<const VkBaseInStructure*>(pNext);\n'
        self.ser_pnext += self.indent + f'switch (pNextStruct->sType) {{\n'

    def endFile(self):
        self.ser_pnext += 2 * self.indent + f'default: {{\n'
        self.ser_pnext += 3 * self.indent +     f'SerializeToStream(*pNextStruct, stream);\n'
        self.ser_pnext += 3 * self.indent +     f'break;\n'
        self.ser_pnext += 2 * self.indent + f'}}\n'
        self.ser_pnext += self.indent + f'}}\n'
        self.ser_pnext += f'}}\n'

        self.body += self.ser
        self.body += f'\n'
        self.body += self.ser_pnext
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        members = typeinfo.elem.findall('member')

        self.ser += f'void SerializeToStream(const {name}& value, WriteStream& stream) {{\n'

        memberAccess = 'value.'
        for i in range(len(members)):
            member = members[i]
            memberName = safeStr(member.find('name').text).strip()

            self.ser += genParamSerializeToStream(member, self.registry, self.indent, memberAccess)
            if memberName == 'sType':
                sTypeValue = member.get('values')
                if sTypeValue is not None:
                    enumProtect = None
                    enumInfo = self.registry.lookupElementInfo(sTypeValue, self.registry.enumdict)
                    if enumInfo is not None:
                        enumElem = enumInfo.elem
                        enumProtect = enumElem.get('protect')

                    if enumProtect:
                        self.ser_pnext += f'#ifdef {enumProtect}\n'

                    self.ser_pnext += 2 * self.indent + f'case {sTypeValue}: {{\n'
                    self.ser_pnext += 3 * self.indent +     f'const {name}* pValue = reinterpret_cast<const {name}*>(pNext);\n'
                    self.ser_pnext += 3 * self.indent +     f'SerializeToStream(*pValue, stream);\n'
                    self.ser_pnext += 3 * self.indent +     f'break;\n'
                    self.ser_pnext += 2 * self.indent + f'}}\n'

                    if enumProtect:
                        self.ser_pnext += f'#endif\n'

        self.ser += f'}}\n\n'


class COutputGeneratorStructDeepCopyDeclaration(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <Vulkan.h>\n'
        self.body += f'#include <SignatureUtils.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.body += f'void DeepCopy(const VkLayerInstanceCreateInfo& valueIn, Allocator& allocator, VkLayerInstanceCreateInfo& valueOut);\n'
        self.body += f'void DeepCopy(const VkLayerDeviceCreateInfo& valueIn, Allocator& allocator, VkLayerDeviceCreateInfo& valueOut);\n'
        self.body += f'void DeepCopyPNext(const void* pNextIn, Allocator& allocator, void** ppNextOut);\n'
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        self.body += f'void DeepCopy(const {name}& valueIn, Allocator& allocator, {name}& valueOut);\n'


class COutputGeneratorStructDeepCopy(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '
        self.copy_struct = ''
        self.copy_pnext = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <StructDeepCopyGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

        self.copy_pnext += f'void DeepCopyPNext(const void* pNextIn, Allocator& allocator, void** ppNextOut) {{\n'
        self.copy_pnext += self.indent + f'if (!ppNextOut) {{\n'
        self.copy_pnext += 2 * self.indent + f'return;\n'
        self.copy_pnext += self.indent + f'}}\n\n'

        self.copy_pnext += self.indent + f'if (!pNextIn) {{\n'
        self.copy_pnext += 2 * self.indent + f'*ppNextOut = nullptr;\n'
        self.copy_pnext += 2 * self.indent + f'return;\n'
        self.copy_pnext += self.indent + f'}}\n\n'

        self.copy_pnext += self.indent + f'const VkBaseInStructure* pNextStruct = reinterpret_cast<const VkBaseInStructure*>(pNextIn);\n'
        self.copy_pnext += self.indent + f'switch (pNextStruct->sType) {{\n'

    def endFile(self):
        self.copy_pnext += 2 * self.indent + f'case VK_STRUCTURE_TYPE_LOADER_INSTANCE_CREATE_INFO: {{\n'
        self.copy_pnext += 3 * self.indent +     f'const VkLayerInstanceCreateInfo* pNextStructIn = reinterpret_cast<const VkLayerInstanceCreateInfo*>(pNextStruct);\n'
        self.copy_pnext += 3 * self.indent +     f'VkLayerInstanceCreateInfo* pNextStructOut = allocator.Allocate<VkLayerInstanceCreateInfo>();\n'
        self.copy_pnext += 3 * self.indent +     f'DeepCopy(*pNextStructIn, allocator, *pNextStructOut);\n'
        self.copy_pnext += 3 * self.indent +     f'*ppNextOut = pNextStructOut;\n'
        self.copy_pnext += 3 * self.indent +     f'break;\n'
        self.copy_pnext += 2 * self.indent + f'}}\n'
        self.copy_pnext += 2 * self.indent + f'case VK_STRUCTURE_TYPE_LOADER_DEVICE_CREATE_INFO: {{\n'
        self.copy_pnext += 3 * self.indent +     f'const VkLayerDeviceCreateInfo* pNextStructIn = reinterpret_cast<const VkLayerDeviceCreateInfo*>(pNextStruct);\n'
        self.copy_pnext += 3 * self.indent +     f'VkLayerDeviceCreateInfo* pNextStructOut = allocator.Allocate<VkLayerDeviceCreateInfo>();\n'
        self.copy_pnext += 3 * self.indent +     f'DeepCopy(*pNextStructIn, allocator, *pNextStructOut);\n'
        self.copy_pnext += 3 * self.indent +     f'*ppNextOut = pNextStructOut;\n'
        self.copy_pnext += 3 * self.indent +     f'break;\n'
        self.copy_pnext += 2 * self.indent + f'}}\n'
        self.copy_pnext += 2 * self.indent + f'default: {{\n'
        self.copy_pnext += 3 * self.indent +     f'const VkBaseInStructure* pNextStructIn = pNextStruct;\n'
        self.copy_pnext += 3 * self.indent +     f'VkBaseInStructure* pNextStructOut = allocator.Allocate<VkBaseInStructure>();\n'
        self.copy_pnext += 3 * self.indent +     f'DeepCopy(*pNextStructIn, allocator, *pNextStructOut);\n'
        self.copy_pnext += 3 * self.indent +     f'*ppNextOut = pNextStructOut;\n'
        self.copy_pnext += 3 * self.indent +     f'break;\n'
        self.copy_pnext += 2 * self.indent + f'}}\n'
        self.copy_pnext += self.indent + f'}}\n'
        self.copy_pnext += f'}}\n'

        self.copy_struct += f'void DeepCopy(const VkLayerInstanceCreateInfo& valueIn, Allocator& allocator, VkLayerInstanceCreateInfo& valueOut) {{\n'
        self.copy_struct += self.indent + f'valueOut = valueIn;\n'
        self.copy_struct += self.indent + f'DeepCopyPNext(valueIn.pNext, allocator, (void**)&valueOut.pNext);\n'
        self.copy_struct += f'}}\n'
        self.copy_struct += f'\n'
        self.copy_struct += f'void DeepCopy(const VkLayerDeviceCreateInfo& valueIn, Allocator& allocator, VkLayerDeviceCreateInfo& valueOut) {{\n'
        self.copy_struct += self.indent + f'valueOut = valueIn;\n'
        self.copy_struct += self.indent + f'DeepCopyPNext(valueIn.pNext, allocator, (void**)&valueOut.pNext);\n'
        self.copy_struct += f'}}\n'

        self.body += self.copy_struct
        self.body += f'\n'
        self.body += self.copy_pnext
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        members = typeinfo.elem.findall('member')

        self.copy_struct += f'void DeepCopy(const {name}& valueIn, Allocator& allocator, {name}& valueOut) {{\n'

        memberInAccess = 'valueIn.'
        memberOutAccess = 'valueOut.'
        for i in range(len(members)):
            member = members[i]
            memberName = safeStr(member.find('name').text).strip()

            self.copy_struct += genParamDeepCopy(member, self.registry, self.indent, memberInAccess, memberOutAccess)
            if memberName == 'sType':
                sTypeValue = member.get('values')
                if sTypeValue is not None:
                    enumProtect = None
                    enumInfo = self.registry.lookupElementInfo(sTypeValue, self.registry.enumdict)
                    if enumInfo is not None:
                        enumElem = enumInfo.elem
                        enumProtect = enumElem.get('protect')

                    if enumProtect:
                        self.copy_pnext += f'#ifdef {enumProtect}\n'

                    self.copy_pnext += 2 * self.indent + f'case {sTypeValue}: {{\n'
                    self.copy_pnext += 3 * self.indent +     f'const {name}* pNextStructIn = reinterpret_cast<const {name}*>(pNextStruct);\n'
                    self.copy_pnext += 3 * self.indent +     f'{name}* pNextStructOut = allocator.Allocate<{name}>();\n'
                    self.copy_pnext += 3 * self.indent +     f'DeepCopy(*pNextStructIn, allocator, *pNextStructOut);\n'
                    self.copy_pnext += 3 * self.indent +     f'*ppNextOut = pNextStructOut;\n'
                    self.copy_pnext += 3 * self.indent +     f'break;\n'
                    self.copy_pnext += 2 * self.indent + f'}}\n'

                    if enumProtect:
                        self.copy_pnext += f'#endif\n'

        self.copy_struct += f'}}\n\n'


class COutputGeneratorStructDeserializeFromStream(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '
        self.deser = ''
        self.deser_pnext = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <StructSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

        self.deser_pnext += f'void DeserializeFromStreamPNext(void** ppNext, Allocator& allocator, const ReadStream& stream) {{\n'
        self.deser_pnext += self.indent + f'if (!ppNext) {{\n'
        self.deser_pnext += 2 * self.indent + f'return;\n'
        self.deser_pnext += self.indent + f'}}\n\n'
        self.deser_pnext += self.indent + f'uint64_t size = 0;\n'
        self.deser_pnext += self.indent + f'DeserializeFromStream(size, allocator, stream);\n\n'
        self.deser_pnext += self.indent + f'if (size == 0) {{\n'
        self.deser_pnext += 2 * self.indent + f'*ppNext = nullptr;\n'
        self.deser_pnext += 2 * self.indent + f'return;\n'
        self.deser_pnext += self.indent + f'}}\n\n'

        self.deser_pnext += self.indent + f'VkBaseInStructure nextStruct{{}};\n'
        self.deser_pnext += self.indent + f'stream.ReadNoAdvance(nextStruct);\n'
        self.deser_pnext += self.indent + f'switch (nextStruct.sType) {{\n'

    def endFile(self):
        self.deser_pnext += 2 * self.indent + f'default: {{\n'
        self.deser_pnext += 3 * self.indent +     f'VkBaseInStructure* pNext = allocator.Allocate<VkBaseInStructure>();\n'
        self.deser_pnext += 3 * self.indent +     f'DeserializeFromStream(*pNext, allocator, stream);\n'
        self.deser_pnext += 3 * self.indent +     f'*ppNext = pNext;\n'
        self.deser_pnext += 3 * self.indent +     f'break;\n'
        self.deser_pnext += 2 * self.indent + f'}}\n'
        self.deser_pnext += self.indent + f'}}\n'
        self.deser_pnext += f'}}\n'

        self.body += self.deser
        self.body += f'\n'
        self.body += self.deser_pnext
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genType(self, typeinfo, name, alias):
        OutputGenerator.genType(self, typeinfo, name, alias)

        category = typeinfo.elem.get('category')
        if category not in ('struct', 'union') or alias:
            return

        members = typeinfo.elem.findall('member')

        self.deser += f'void DeserializeFromStream({name}& value, Allocator& allocator, const ReadStream& stream) {{\n'

        memberAccess = 'value.'
        for i in range(len(members)):
            member = members[i]
            memberName = safeStr(member.find('name').text).strip()

            self.deser += genParamDeserializeFromStream(member, self.registry, self.indent, memberAccess)
            if memberName == 'sType':
                sTypeValue = member.get('values')
                if sTypeValue is not None:
                    enumProtect = None
                    enumInfo = self.registry.lookupElementInfo(sTypeValue, self.registry.enumdict)
                    if enumInfo is not None:
                        enumElem = enumInfo.elem
                        enumProtect = enumElem.get('protect')

                    if enumProtect:
                        self.deser_pnext += f'#ifdef {enumProtect}\n'

                    self.deser_pnext += 2 * self.indent + f'case {sTypeValue}: {{\n'
                    self.deser_pnext += 3 * self.indent +     f'{name}* pNext = allocator.Allocate<{name}>();\n'
                    self.deser_pnext += 3 * self.indent +     f'DeserializeFromStream(*pNext, allocator, stream);\n'
                    self.deser_pnext += 3 * self.indent +     f'*ppNext = pNext;\n'
                    self.deser_pnext += 3 * self.indent +     f'break;\n'
                    self.deser_pnext += 2 * self.indent + f'}}\n'

                    if enumProtect:
                        self.deser_pnext += f'#endif\n'

        self.deser += f'}}\n\n'


class COutputGeneratorEnumSerializeDeclaration(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '
        self.group_enums = ''
        self.bitmask_enums = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <Vulkan.h>\n\n'
        self.body += f'#include <sstream>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.body += self.group_enums
        self.body += f'\n'
        self.body += self.bitmask_enums
        self.body += f'\n'
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genGroup(self, groupinfo, groupName, alias=None):
        OutputGenerator.genGroup(self, groupinfo, groupName, alias)

        if alias:
            return

        groupElem = groupinfo.elem
        if groupElem.get('type') == "enum":
            self.group_enums += f'void SerializeToString({groupName} value, std::stringstream& stream);\n'
        else:
            flags = groupName.replace('FlagBits', 'Flags')
            self.bitmask_enums += f'void SerializeToString{flags}({flags} value, std::stringstream& stream);\n'


class COutputGeneratorEnumSerialize(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '
        self.group_enums = ''
        self.bitmask_enums = ''

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <EnumSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.body += self.group_enums
        self.body += f'\n'
        self.body += self.bitmask_enums
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genGroup(self, groupinfo, groupName, alias=None):
        OutputGenerator.genGroup(self, groupinfo, groupName, alias)

        if alias:
            return

        groupElem = groupinfo.elem
        if groupElem.get('type') == "enum":
            self.group_enums += f'void SerializeToString({groupName} value, std::stringstream& stream) {{\n'
            self.group_enums += self.indent + f'switch (value) {{\n'
            self.group_enums += 2 * self.indent + f'default: stream << value; break;\n'

            enums = groupElem.findall('enum')
            enums = self.checkDuplicateEnums(enums)
            for enum in enums:
                enumAlias = enum.get('alias')
                if enumAlias:
                    continue

                enumName = enum.get('name')
                enumValue = enum.get('value')
                enumProtect = enum.get('protect')
                enumRequired = enum.get('required')
                if enumRequired is None:
                    continue

                if enumProtect:
                    self.group_enums += f'#ifdef {enumProtect}\n'

                self.group_enums += 2 * self.indent + f'case {enumName}: stream << \"{enumName}\"; break;\n'

                if enumProtect:
                    self.group_enums += f'#endif\n'

            self.group_enums += self.indent + f'}}\n'
            self.group_enums += f'}}\n\n'
        else:
            zeroEnumName = ''

            flags = groupName.replace('FlagBits', 'Flags')
            self.bitmask_enums += f'void SerializeToString{flags}({flags} value, std::stringstream& stream) {{\n'

            enums = groupElem.findall('enum')
            enums = self.checkDuplicateEnums(enums)
            for enum in enums:
                enumAlias = enum.get('alias')
                if enumAlias:
                    continue

                enumName = enum.get('name')
                enumValue = enum.get('value')
                enumBitpos = enum.get('bitpos')
                enumProtect = enum.get('protect')
                enumRequired = enum.get('required')
                if enumRequired is None:
                    continue
                if enumValue is None:
                    continue

                numValue = int(enumValue, 16)
                if numValue == 0:
                    zeroEnumName = enumName

                    if enumProtect:
                        self.bitmask_enums += f'#ifdef {enumProtect}\n'

                    self.bitmask_enums += self.indent + f'if (value == {enumName}) {{\n'
                    self.bitmask_enums += 2 * self.indent + f'stream << \"{enumName}\";\n'
                    self.bitmask_enums += 2 * self.indent + f'return;\n'
                    self.bitmask_enums += self.indent + f'}}\n\n'

                    if enumProtect:
                        self.bitmask_enums += f'#endif\n'

            if zeroEnumName == '':
                self.bitmask_enums += self.indent + f'if (value == 0) {{\n'
                self.bitmask_enums += 2 * self.indent + f'stream << 0;\n'
                self.bitmask_enums += 2 * self.indent + f'return;\n'
                self.bitmask_enums += self.indent + f'}}\n\n'

            self.bitmask_enums += self.indent + f'std::string out;\n\n'
            for enum in enums:
                enumAlias = enum.get('alias')
                if enumAlias:
                    continue

                enumName = enum.get('name')
                enumValue = enum.get('value')
                enumBitpos = enum.get('bitpos')
                enumProtect = enum.get('protect')
                enumRequired = enum.get('required')
                if enumRequired is None:
                    continue

                if enumName == zeroEnumName:
                    continue

                if enumProtect:
                    self.bitmask_enums += f'#ifdef {enumProtect}\n'

                self.bitmask_enums += self.indent + f'if (value & {enumName}) {{\n'
                self.bitmask_enums += 2 * self.indent + f'if (!out.empty()) {{ out.push_back(\'|\'); }}\n'
                self.bitmask_enums += 2 * self.indent + f'out += \"{enumName}\";\n'
                self.bitmask_enums += 2 * self.indent + f'value &= ~{enumName};\n'
                self.bitmask_enums += self.indent + f'}}\n'

                if enumProtect:
                    self.bitmask_enums += f'#endif\n'

            self.bitmask_enums += self.indent + f'if (value) {{\n'
            self.bitmask_enums += 2 * self.indent + f'if (!out.empty()) {{ out.push_back(\'|\'); }}\n'
            self.bitmask_enums += 2 * self.indent + f'out += \"UNKNOWN_BITS(\";\n'
            self.bitmask_enums += 2 * self.indent + f'out += std::to_string(value);\n'
            self.bitmask_enums += 2 * self.indent + f'out += \")\";\n'
            self.bitmask_enums += self.indent + f'}}\n\n'
            self.bitmask_enums += self.indent + f'stream << out;\n'
            self.bitmask_enums += f'}}\n\n'


class COutputGeneratorSignatureSerializeToString(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <SignatureGenerated.h>\n'
        self.body += f'#include <EnumSerializeGenerated.h>\n'
        self.body += f'#include <StructSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        self.body += f'void SerializeToString(const {name}Signature& sig, std::stringstream& stream) {{\n'
        self.body += self.indent + f'stream << \"{name}(\";\n'

        paramAccess = 'sig.'
        for i in range(len(params)):
            param = params[i]

            if i > 0:
                self.body += self.indent + f'stream << \", \";\n'

            self.body += genParamSerializeToString(param, self.registry, self.indent, paramAccess)

        self.body += self.indent + f'stream << \")\";\n'
        if type != 'void':
            self.body += self.indent + f'stream << \" -> \";\n'

            cast = ''
            customSerializer = ''
            retParamTypeKind = None
            typeElem = getNonAliasTypeElem(type, self.registry)
            category = safeStr(typeElem.get('category'))
            requires = safeStr(typeElem.get('requires'))
            if category == 'funcpointer':
                retParamTypeKind = ParamTypeKind.FUNCTION_PTR
                cast = '(void*)'
            if type in BASE_TYPES or category in ['basetype', 'enum', 'bitmask']:
                retParamTypeKind = ParamTypeKind.PRIMITIVE
                if category == 'bitmask' and requires != '':
                    customSerializer = typeElem.get('name')

            if DEBUG_PARAM_TYPE_KIND:
                self.body += self.indent + f'// Kind: {retParamTypeKind.name}\n'

            self.body += self.indent + f'SerializeToString{customSerializer}({cast}{paramAccess}ret, stream);\n'

        self.body += f'}};\n\n'


class COutputGeneratorSignatureSerializeToStream(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <SignatureGenerated.h>\n'
        self.body += f'#include <StructSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        self.body += f'void SerializeToStream(const {name}Signature& sig, WriteStream& stream) {{\n'
        self.body += self.indent + f'SerializeToStream(sig.header, stream);\n'

        paramAccess = 'sig.'
        for i in range(len(params)):
            param = params[i]

            self.body += genParamSerializeToStream(param, self.registry, self.indent, paramAccess)

        if type != 'void':
            retParamTypeKind = None
            typeElem = getNonAliasTypeElem(type, self.registry)
            category = safeStr(typeElem.get('category'))
            requires = safeStr(typeElem.get('requires'))
            if category == 'funcpointer':
                retParamTypeKind = ParamTypeKind.FUNCTION_PTR
            if type in BASE_TYPES or category in ['basetype', 'enum', 'bitmask']:
                retParamTypeKind = ParamTypeKind.PRIMITIVE

            if DEBUG_PARAM_TYPE_KIND:
                self.body += self.indent + f'// Kind: {retParamTypeKind.name}\n'

            self.body += self.indent + f'SerializeToStream({paramAccess}ret, stream);\n'

        self.body += f'}};\n\n'


class COutputGeneratorSignatureDeserializeFromStream(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.body += f'#include <SignatureUtils.h>\n'
        self.body += f'#include <SignatureGenerated.h>\n'
        self.body += f'#include <StructSerializeGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n'
        self.body += f'namespace SignatureSerializer {{\n\n'

    def endFile(self):
        self.body += f'}} // namespace SignatureSerializer\n'
        self.body += f'}} // namespace OVS'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        self.body += f'void DeserializeFromStream({name}Signature& sig, const ReadStream& stream) {{\n'
        self.body += self.indent + f'auto& allocator = sig.allocator;\n'
        self.body += self.indent + f'DeserializeFromStream(sig.header, allocator, stream);\n'

        paramAccess = 'sig.'
        for i in range(len(params)):
            param = params[i]

            self.body += genParamDeserializeFromStream(param, self.registry, self.indent, paramAccess)

        if type != 'void':
            retParamTypeKind = None
            typeElem = getNonAliasTypeElem(type, self.registry)
            category = safeStr(typeElem.get('category'))
            requires = safeStr(typeElem.get('requires'))
            if category == 'funcpointer':
                retParamTypeKind = ParamTypeKind.FUNCTION_PTR
            if type in BASE_TYPES or category in ['basetype', 'enum', 'bitmask']:
                retParamTypeKind = ParamTypeKind.PRIMITIVE

            if DEBUG_PARAM_TYPE_KIND:
                self.body += self.indent + f'// Kind: {retParamTypeKind.name}\n'

            self.body += self.indent + f'DeserializeFromStream({paramAccess}ret, allocator, stream);\n'

        self.body += f'}};\n\n'


class COutputGeneratorVulkanLayerAPIDump(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <VulkanLayerInterfaceGenerated.h>\n\n'
        self.body += f'#include <SignatureGenerated.h>\n\n'
        self.body += f'#include <iostream>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'class VulkanLayerAPIDump : public VulkanLayerInterface {{\n'
        self.body += f'public:\n'

    def endFile(self):
        self.body += self.indent + f'explicit VulkanLayerAPIDump(const VulkanLayerAPIDumpSettings& settings) : VulkanLayerInterface(VulkanLayerType::APIDump), settings_{{settings}} {{\n'
        self.body += 2 * self.indent + f'std::streambuf* buf = std::cout.rdbuf();\n'
        self.body += 2 * self.indent + f'if (settings_.filename != \"stdout\") {{\n'
        self.body += 3 * self.indent +     f'fout_ = std::ofstream(settings_.filename);\n'
        self.body += 3 * self.indent +     f'buf = fout_.rdbuf();\n'
        self.body += 2 * self.indent + f'}}\n'
        self.body += 2 * self.indent + f'out_.set_rdbuf(buf);\n'
        self.body += self.indent + f'}}\n\n'
        self.body += self.indent + f'virtual ~VulkanLayerAPIDump() {{}}\n\n'
        self.body += f'private:\n'
        self.body += self.indent + f'VulkanLayerAPIDumpSettings settings_{{}};\n'
        self.body += self.indent + f'std::ofstream fout_;\n'
        self.body += self.indent + f'std::ostream out_{{std::cout.rdbuf()}};\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        call = ''
        sign = ''

        call += 2 * self.indent
        if type != 'void':
            call += f'{type} ret = '
        call += f'next_->{name}('
        sign += 2 * self.indent + f'SignatureSerializer::{name}Signature sig{{}};\n'

        paramAccess = 'sig.'
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            paramTypeKind = getParamTypeKind(param, self.registry)

            if i > 0:
                decl += f', '
                call += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()
            call += f'{param_name}'
            if ('const' in param_qual or 'const' in param_type) and ('*' in param_type):
                sign += 2 * self.indent + f'{paramAccess}{param_name} = const_cast<{param_type}>({param_name});\n'
            elif paramTypeKind in [ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D, ParamTypeKind.HANDLE_STATIC_ARRAY_1D, ParamTypeKind.STRUCT_STATIC_ARRAY_1D]:
                sign += 2 * self.indent + f'std::memcpy({paramAccess}{param_name}, {param_name}, sizeof({paramAccess}{param_name}));\n'
            else:
                sign += 2 * self.indent + f'{paramAccess}{param_name} = {param_name};\n'

        call += ');\n'

        if type != 'void':
            sign += 2 * self.indent + f'{paramAccess}ret = ret;\n'

        self.body += self.indent + f'virtual {type} {name}({decl}) override {{\n'
        self.body += f'{call}\n'
        self.body += f'{sign}\n'
        self.body += 2 * self.indent + f'std::stringstream stream;\n'
        self.body += 2 * self.indent + f'SignatureSerializer::SerializeToString(sig, stream);\n'
        self.body += 2 * self.indent + f'out_ << stream.str() << \'\\n\';\n'
        if type != 'void':
            self.body += 2 * self.indent + f'return ret;\n'

        self.body += self.indent + f'}}\n\n'


class COutputGeneratorVulkanLayerAPITrace(OutputGenerator):
    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)

        self.body = ''
        self.header_guard = ''
        self.indent = '    '

    def beginFile(self, genOpts):
        OutputGenerator.beginFile(self, genOpts)

        self.body += autogen_message

        self.header_guard = getMacroForFilename(self.genOpts.filename)
        self.body += f'#ifndef {self.header_guard}\n'
        self.body += f'#define {self.header_guard}\n\n'
        self.body += f'#include <PlatformUtils.h>\n\n'
        self.body += f'#include <VulkanLayerInterfaceGenerated.h>\n\n'
        self.body += f'#include <SignatureGenerated.h>\n\n'
        self.body += f'namespace OVS {{\n\n'
        self.body += f'class VulkanLayerAPITrace : public VulkanLayerInterface {{\n'
        self.body += f'public:\n'

    def endFile(self):
        self.body += self.indent + f'explicit VulkanLayerAPITrace(const VulkanLayerAPITraceSettings& settings) : VulkanLayerInterface(VulkanLayerType::APITrace), settings_{{settings}}, buffered_(settings.flushSize) {{\n'
        self.body += 2 * self.indent + f'file_ = std::fopen(settings_.filename.c_str(), \"wb\");\n'
        self.body += self.indent + f'}}\n\n'

        self.body += self.indent + f'virtual ~VulkanLayerAPITrace() {{\n'
        self.body += 2 * self.indent + f'std::lock_guard lock(bufferedMutex_);\n'
        self.body += 2 * self.indent + f'std::fwrite(buffered_.data(), sizeof(uint8_t), bufferedSize_, file_);\n'
        self.body += 2 * self.indent + f'std::fclose(file_);\n'
        self.body += self.indent + f'}}\n\n'

        self.body += f'private:\n'

        self.body += self.indent + f'void SaveData(std::vector<uint8_t>& data) {{\n'
        self.body += 2 * self.indent + f'std::lock_guard lock(bufferedMutex_);\n\n'
        self.body += 2 * self.indent + f'auto* header = reinterpret_cast<SignatureSerializer::SignatureHeader*>(data.data());\n'
        self.body += 2 * self.indent + f'header->thread = GetThreadID();\n'
        self.body += 2 * self.indent + f'header->byteSize = data.size();\n'
        self.body += 2 * self.indent + f'header->globalIndex = signatureIndex_++;\n\n'
        self.body += 2 * self.indent + f'if (bufferedSize_ + settings_.flushSize > buffered_.size()) {{\n'
        self.body += 3 * self.indent +     f'buffered_.resize(bufferedSize_ + settings_.flushSize);\n'
        self.body += 2 * self.indent + f'}}\n'
        self.body += 2 * self.indent + f'std::memcpy(buffered_.data() + bufferedSize_, data.data(), data.size());\n'
        self.body += 2 * self.indent + f'bufferedSize_ += data.size();\n\n'
        self.body += 2 * self.indent + f'if (bufferedSize_ >= settings_.flushSize) {{\n'
        self.body += 3 * self.indent +     f'std::fwrite(buffered_.data(), sizeof(uint8_t), bufferedSize_, file_);\n'
        self.body += 3 * self.indent +     f'bufferedSize_ = 0;\n'
        self.body += 2 * self.indent + f'}}\n'
        self.body += self.indent + f'}}\n\n'

        self.body += self.indent + f'VulkanLayerAPITraceSettings settings_{{}};\n'
        self.body += self.indent + f'std::vector<uint8_t> buffered_;\n'
        self.body += self.indent + f'std::size_t bufferedSize_{{0}};\n'
        self.body += self.indent + f'std::mutex bufferedMutex_;\n'
        self.body += self.indent + f'std::FILE* file_{{nullptr}};\n'
        self.body += self.indent + f'uint64_t signatureIndex_{{0}};\n'
        self.body += f'}};\n\n'
        self.body += f'}} // namespace OVS\n\n'
        self.body += f'#endif // {self.header_guard}'

        self.outFile.write(self.body)

        OutputGenerator.endFile(self)

    def genCmd(self, cmdinfo, name, alias):
        OutputGenerator.genCmd(self, cmdinfo, name, alias)

        proto = cmdinfo.elem.find('proto')
        type = proto.find('type').text.strip()
        params = cmdinfo.elem.findall('param')

        decl = ''
        call = ''
        sign = ''

        call += 2 * self.indent
        if type != 'void':
            call += f'{type} ret = '
        call += f'next_->{name}('
        sign += 2 * self.indent + f'SignatureSerializer::{name}Signature sig{{}};\n'
        sign += 2 * self.indent + f'sig.header.callID = APICallID::{name};\n'

        paramAccess = 'sig.'
        for i in range(len(params)):
            param = params[i]
            param_qual = safeStr(param.text).strip()
            param_type = safeStr(param.find('type').text).strip() + safeStr(param.find('type').tail).strip()
            param_name = safeStr(param.find('name').text).strip()
            param_tail = safeStr(param.find('name').tail).strip()

            paramTypeKind = getParamTypeKind(param, self.registry)

            if i > 0:
                decl += f', '
                call += f', '

            decl += f'{param_qual} {param_type} {param_name}{param_tail}'.strip()
            call += f'{param_name}'
            if ('const' in param_qual or 'const' in param_type) and ('*' in param_type):
                sign += 2 * self.indent + f'{paramAccess}{param_name} = const_cast<{param_type}>({param_name});\n'
            elif paramTypeKind in [ParamTypeKind.PRIMITIVE_STATIC_ARRAY_1D, ParamTypeKind.HANDLE_STATIC_ARRAY_1D, ParamTypeKind.STRUCT_STATIC_ARRAY_1D]:
                sign += 2 * self.indent + f'std::memcpy({paramAccess}{param_name}, {param_name}, sizeof({paramAccess}{param_name}));\n'
            else:
                sign += 2 * self.indent + f'{paramAccess}{param_name} = {param_name};\n'

        call += ');\n'

        if type != 'void':
            sign += 2 * self.indent + f'{paramAccess}ret = ret;\n'

        self.body += self.indent + f'virtual {type} {name}({decl}) override {{\n'
        self.body += f'{call}\n'
        self.body += f'{sign}\n'
        self.body += 2 * self.indent + f'std::vector<uint8_t> sigdata;\n'
        self.body += 2 * self.indent + f'SignatureSerializer::WriteStream stream(sigdata);\n'
        self.body += 2 * self.indent + f'SignatureSerializer::SerializeToStream(sig, stream);\n'
        self.body += 2 * self.indent + f'SaveData(sigdata);\n'
        if type != 'void':
            self.body += 2 * self.indent + f'return ret;\n'

        self.body += self.indent + f'}}\n\n'


generate_targets = [
    [COutputGeneratorCustom,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanCoreGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanDispatchTables,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanDispatchTablesGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerEntry,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerEntryGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerInterface,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerInterfaceGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerTerminatorBase,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerTerminatorBaseGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerPrinter,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerPrinterGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerPassThrough,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerPassThroughGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanAPICallID,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanAPICallIDGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanAPIStructID,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanAPIStructIDGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorSignatureDeclaration,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='SignatureGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorStructSerializeDeclaration,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='StructSerializeGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorStructSerializeToString,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='StructSerializeToStringGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorStructSerializeToStream,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='StructSerializeToStreamGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorStructDeserializeFromStream,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='StructDeserializeFromStreamGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorEnumSerializeDeclaration,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='EnumSerializeGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorEnumSerialize,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='EnumSerializeGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorSignatureSerializeToString,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='SignatureSerializeToStringGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorSignatureSerializeToStream,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='SignatureSerializeToStreamGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorSignatureDeserializeFromStream,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='SignatureDeserializeFromStreamGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerAPIDump,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerAPIDumpGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorVulkanLayerAPITrace,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='VulkanLayerAPITraceGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorStructDeepCopyDeclaration,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='StructDeepCopyGenerated.h',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],

    [COutputGeneratorStructDeepCopy,
     CGeneratorOptions(prefixText='',
                       apicall='VKAPI_ATTR ',
                       apientry='VKAPI_CALL ',
                       apientryp='VKAPI_PTR *',
                       indentFuncProto=True,
                       protectFeature=False,
                       conventions=vkconventions.VulkanConventions(),
                       filename='StructDeepCopyGenerated.cpp',
                       directory='.',
                       apiname='vulkan',
                       defaultExtensions='vulkan',
                       emitExtensions = '.*')],
]


def main():
    with open('Diag.txt', 'w') as diagFile:
        for index, target in enumerate(generate_targets):
            gen = target[0]
            options = target[1]

            index = index + 1
            targets_count = len(generate_targets)
            print(f'[{index:>2}/{targets_count}] Generating target \'{options.filename}\'')

            gen = gen(errFile=diagFile, warnFile=diagFile, diagFile=diagFile)

            registry = reg.Registry(gen, options)

            tree = etree.parse(REGISTRY_PATH)
            registry.loadElementTree(tree)

            if not isinstance(gen, COutputGeneratorCustom):
                video_tree = etree.parse(VIDEO_REGISTRY_PATH)
                addVideoTree(registry, video_tree)

            registry.apiGen()


if __name__ == '__main__':
    main()